apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: app
  name: app
spec:
  ports:
    - name: "web app port"
      port: 80
      targetPort: 5460
  selector:
    io.kompose.service: app