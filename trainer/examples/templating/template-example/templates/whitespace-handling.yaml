## Since this produces invalid yaml you must render with the --debug flag

## we comment the file out so that the rest of the templates render correctly
## to use simply remove the '#'

# myYaml:
  # k8s: {{ "is-awesome" -}}
  # myValue: foo
  # myOtherValue:
        # {{- .Values.myOtherValue }}
